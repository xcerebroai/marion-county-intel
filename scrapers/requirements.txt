# Marion County adapter dependencies.
# Install into a project-local venv (the framework does not auto-install deps):
#   uv venv .venv --python 3.11
#   uv pip install --python .venv\Scripts\python.exe -r scrapers\requirements.txt
#
# The framework gate (scaffold/tests/run_all.py) has NO external dependencies and
# runs on the system Python. Only these adapters need the venv.

pypdf==6.14.2        # tax sale Parcel Status / Surplus PDFs (recon §4.2)
requests==2.34.2     # HTTP with session reuse

# Required by the FRAMEWORK GATE, not by the adapters: 13 v5.4.0 contract-shape
# tests plus the golden path import jsonschema and hard-fail without it. It
# happened to be present in system Python locally but not in this venv, so the
# gate passed on a workstation and failed in CI. Pinned here so the CI
# environment and the venv both satisfy the gate.
#   Verify the way CI does:  .venv\Scripts\python.exe scaffold\tests\run_all.py
jsonschema==4.26.0
# playwright is installed separately (browser download):
#   uv pip install --python .venv\Scripts\python.exe playwright
#   .venv\Scripts\python.exe -m playwright install chromium
# Needed only by scrapers/recorder_fidlar.py — recon §4.1 established that
# POST /breeze/Search returns 401 without browser-minted headers.
